import { Collection } from '@discordjs/collection';
import { RequestHandler } from '.';
import type { Client, ExtendedRequestData } from '../lib';
export declare class RestManager {
    client: Client;
    baseURL: string;
    requestHandlers: Collection<string, RequestHandler>;
    constructor(client: Client);
    get routeBuilder(): any;
    request(method: string, path: string, options: ExtendedRequestData<unknown, unknown>): Promise<any>;
}
//# sourceMappingURL=RestManager.d.ts.map