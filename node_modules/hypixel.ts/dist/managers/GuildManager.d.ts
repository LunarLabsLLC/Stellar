import { BaseManager } from '.';
import type { Guild } from '../classes';
import { type Client } from '../lib';
export declare class GuildManager extends BaseManager {
    constructor(client: Client);
    fetch(id: string, type: 'id' | 'player' | 'name'): Promise<Guild>;
}
//# sourceMappingURL=GuildManager.d.ts.map